# Copy this file to .env and fill in real values. .env is gitignored — never commit real secrets.

# Get a key at https://resend.com/api-keys
RESEND_API_KEY=

# Where contact form messages should be delivered
CONTACT_RECEIVER_EMAIL=pratimaverma711@gmail.com

# Must be a sender Resend allows you to send from.
# Resend's shared testing address works with no domain setup:
CONTACT_SENDER_EMAIL=onboarding@resend.dev
